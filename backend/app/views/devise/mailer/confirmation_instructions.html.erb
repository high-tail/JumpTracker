<p>JumpTrackerに登録ありがとうございます!</p>

<p>下のリンクを押して、登録を完了してください</p>

<p><%= link_to '登録する', confirmation_url(@resource, {confirmation_token: @token, config: message['client-config'].to_s, redirect_url: message['redirect-url']}).html_safe %></p>

build:
  docker:
    api: Dockerfile

release:
  command:
    - bundle exec rake db:migrate

run:
  api: bundle exec puma -t 5:5 -e ${RAILS_ENV:-production} -p ${PORT:-3000} -C config/puma.rb config.ru